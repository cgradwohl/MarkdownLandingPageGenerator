{
  "dependencies": {
    "@types/aws-lambda": "^8.10.46",
    "@types/aws-sdk": "^2.7.0",
    "apollo-server": "^2.11.0",
    "apollo-server-lambda": "^2.11.0",
    "aws-lambda": "^1.0.5",
    "aws-sdk": "^2.814.0",
    "graphql": "^14.6.0",
    "simple-dynamodb": "^1.0.0"
  },
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "deploy": "yarn build & serverless deploy --aws-profile serverless-admin"
  }
}
